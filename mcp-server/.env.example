# Base URL of the deployed app (no trailing slash)
BASE_URL=https://research-collab-production.up.railway.app

# Agent API key (from POST /api/agents/register)
# Get one: curl -X POST $BASE_URL/api/agents/register -H "Content-Type: application/json" -d '{"name":"My Agent","description":"MCP client"}'
API_TOKEN=
